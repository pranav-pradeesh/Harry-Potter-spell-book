<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.magicspells.hpspellbook">

    <application
        android:label="Harry Potter Spellbook"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@style/Theme.NoActionBar">

        <!-- Persistent foreground microphone service -->
        <service
            android:name="org.kivy.android.PythonService"
            android:process=":voiceservice"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="microphone" />

        <!-- Restart service on boot -->
        <receiver
            android:name="org.kivy.android.PythonServiceAutoStart"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </receiver>

        <!-- Main activity -->
        <activity
            android:name="org.kivy.android.PythonActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:screenOrientation="portrait"
            android:showOnLockScreen="true"
            android:turnScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Metadata to keep service sticky -->
        <meta-data android:name="SERVICE_NAME" android:value="VoiceService"/>
        <meta-data android:name="SERVICE_STICKY" android:value="true"/>

    </application>
</manifest>
